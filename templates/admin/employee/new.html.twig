{% extends 'admin/base.html.twig' %}

{% block title %}New employee{% endblock %}

{% block body %}
    <div class="card shadow-sm p-4">
        <h1 class="mb-4">Nowy pracownik</h1>

        {{ include('admin/employee/_form.html.twig') }}

        <a href="{{ path('app_employee_index') }}" class="btn btn-secondary mt-3">
            Back to list
        </a>
    </div>
{% endblock %}
